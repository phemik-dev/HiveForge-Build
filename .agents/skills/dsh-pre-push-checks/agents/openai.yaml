interface:
  display_name: "DSH Pre-Push Checks"
  short_description: "Run the relevant HiveForge Harness checks before push"
  default_prompt: "Use $dsh-pre-push-checks before pushing this HiveForge Harness branch."
