# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@hiveforge-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@hiveforge-ai/dsh-cordis-host-runner'
          - id: tool-cordis
            name: '@hiveforge-ai/dsh-tool-cordis'
