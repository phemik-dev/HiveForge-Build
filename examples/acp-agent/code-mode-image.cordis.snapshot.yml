# Keyless replay combines Code Mode with the durable image store and an exact
# image-capable replay route. The scenario generates its tiny PNG inside the
# run_code program, then exercises read_image as a nested dispatch.
- id: base
  name: '@hiveforge-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-hiveforge
        name: '@hiveforge-ai/dsh-llm-hiveforge'
        disabled: true
      - id: acp-agent
        name: '@hiveforge-ai/dsh-acp-demo'
        config:
          provider: hiveforge-official
          model: hiveforge-v4-flash-vision-exp
          persistenceRoot: !!js process.env.DSH_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
          persistenceCompression: none
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: code
          persona: |
            You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - insert:
          - id: attachment-local
            name: '@hiveforge-ai/dsh-attachment-local'
          - id: code-runtime
            name: '@hiveforge-ai/dsh-code-runtime-worker-thread'
          - id: llm-replay
            name: '@hiveforge-ai/dsh-llm-replay'
            config:
              providers:
                - id: hiveforge-official
                  name: HiveForge
                  models:
                    - id: hiveforge-v4-flash
                      inputModalities: [text]
                    - id: hiveforge-v4-pro
                      inputModalities: [text]
                    - id: hiveforge-v4-flash-vision-exp
                      inputModalities: [text, image]
