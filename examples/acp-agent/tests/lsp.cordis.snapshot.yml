# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@hiveforge-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-hiveforge
        name: '@hiveforge-ai/dsh-llm-hiveforge'
        disabled: true
      - insert:
          - id: lsp
            name: '@hiveforge-ai/dsh-lsp'
          - id: lsp-stdio
            name: '@hiveforge-ai/dsh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@hiveforge-ai/dsh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@hiveforge-ai/dsh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@hiveforge-ai/dsh-llm-replay'
