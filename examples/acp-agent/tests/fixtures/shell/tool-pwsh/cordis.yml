# Minimal tool-pwsh composition: real app boot path, real pwsh executor, real
# foreground + background tool calls; driven by the package's loader.spec.ts.
- id: system-prompt
  name: '@hiveforge-ai/dsh-system-prompt'

- id: tools
  name: '@hiveforge-ai/dsh-tools'

- id: subprocess
  name: '@hiveforge-ai/dsh-subprocess-local'

- id: bash
  name: '@hiveforge-ai/dsh-pwsh-local'
  config:
    graceMs: 200

- id: shell-env
  name: '@hiveforge-ai/dsh-shell-env'

- id: tasks
  name: '@hiveforge-ai/dsh-jobs-local'

- id: tool-jobs
  name: '@hiveforge-ai/dsh-tool-jobs'

- id: tool-pwsh
  name: '@hiveforge-ai/dsh-tool-pwsh'
