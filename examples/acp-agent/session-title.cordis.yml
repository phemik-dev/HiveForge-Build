# Session-title snapshot composition: the optional first-prompt provider uses
# the ordinary HiveForge route while the ACP app and every other capability stay
# identical to the base example.
- id: base
  name: '@hiveforge-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: session-title-provider
            name: '@hiveforge-ai/dsh-session-title-first-prompt-llm'
            config:
              targetWords: 5
              targetCjkCharacters: 10
              maxInputBytes: 4096
              maxOutputTokens: 32
              timeoutMs: 5000
              provider: hiveforge-official
              model: hiveforge-v4-flash
