# Keyless Agent Teams composition over the real headless app and deterministic fixture adapter.
- id: base
  name: '@hiveforge-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-hiveforge
        name: '@hiveforge-ai/dsh-llm-hiveforge'
        disabled: true
      - id: tool-subagent-control
        name: '@hiveforge-ai/dsh-tool-subagent-control'
        disabled: true
      - id: tool-subagent-report
        name: '@hiveforge-ai/dsh-tool-subagent-report'
        disabled: true
      - id: tool-subagent
        name: '@hiveforge-ai/dsh-tool-subagent'
        config:
          provider: spawn
          toolName: subagent
          backgroundMode: one-shot
          maxDepth: 1
      - id: tool-subagent-fork
        name: '@hiveforge-ai/dsh-tool-subagent'
        config:
          provider: fork
          toolName: subagent_fork
          backgroundMode: one-shot
          maxDepth: 1
      - insert:
          - id: agent-team
            name: '@hiveforge-ai/dsh-experimental-agent-team'
          - id: tool-agent-team
            name: '@hiveforge-ai/dsh-experimental-tool-agent-team'
          - id: team-fixture-llm
            name: './tests/fixtures/team-llm.mjs'
