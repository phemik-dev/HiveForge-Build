[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "hiveforge-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for HiveForge Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "HiveForge" }]
dependencies = [
  "pydantic>=2.12,<3",
  "hiveforge-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/hiveforge-ai/hiveforge-harness"
Documentation = "https://github.com/hiveforge-ai/hiveforge-harness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/hiveforge-ai/hiveforge-harness/issues"
Source = "https://github.com/hiveforge-ai/hiveforge-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/hiveforge_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
hiveforge-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
