[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "hiveforge-harness-runtime-bin"
version = "0.0.0.dev0"
description = "Pinned HiveForge Harness runtime for the Python SDK"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "HiveForge" }]

[project.urls]
Homepage = "https://github.com/hiveforge-ai/hiveforge-harness"
Documentation = "https://github.com/hiveforge-ai/hiveforge-harness/blob/master/python/sdk-runtime/README.md"
Issues = "https://github.com/hiveforge-ai/hiveforge-harness/issues"
Source = "https://github.com/hiveforge-ai/hiveforge-harness"

# Include the injected executable and default config; exclude the dev-only node
# closure from wheels and sdists.
[tool.hatch.build]
artifacts = ["src/hiveforge_harness_runtime/runtime/dsh-jsonrpc-agent-*"]
exclude = ["src/hiveforge_harness_runtime/runtime/node"]

[tool.hatch.build.targets.wheel]
packages = ["src/hiveforge_harness_runtime"]

[tool.hatch.build.targets.wheel.hooks.custom]

[tool.hatch.build.targets.sdist.hooks.custom]
